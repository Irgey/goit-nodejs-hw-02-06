const isBodyEmpty = require("./isBodyEmpty");
const isValidId = require("./isValidId");
const isFavoriteExists = require("./isFavoriteExists");
const authenticate = require("./authenticate");
const upload = require("./upload");
module.exports = {
  isBodyEmpty,
  isValidId,
  isFavoriteExists,
  authenticate,
  upload,
};
